import { Flex } from "@chakra-ui/react"

const Footer = () => (
  <>
    <Flex
      padding={{ base: 3, md: 5 }}
      bg="mint.bg"
      align="center"
      color="mint.front"
      justify="space-between"
    >footer</Flex>
  </>
)
export default Footer